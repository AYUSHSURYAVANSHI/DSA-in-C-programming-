#include<stdio.h>
int main()
{
int n,i,j,arr[50],arr[100];
printf("enter the number of elements in array\n");
scanf("%d",&n);
printf("enter array elements\n");
for(i=0;i<n;i++)
   scanf("%d",& arr[i]);

for(i=n-1;j=0;i>= 0; i--,j++)
   arr[i]=arr[j];

for()

return 0;
}